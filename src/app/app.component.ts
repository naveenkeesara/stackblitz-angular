import { Component } from '@angular/core';

@Component({
  selector: 'app-root',
  templateUrl: './app.component.html',
  styleUrls: ['./app.component.css']
})
export class AppComponent {
  // The app shell is ready.
  // Your work starts inside UserListComponent and UserCardComponent.
  // Read the instructions in the browser carefully before starting.
}
